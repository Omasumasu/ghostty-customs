// Terminal IDE レイアウト（Claude Code 3つ + チートシート + フローティングツール）
// 使い方: zellij --layout terminal-ide
//
// メインレイアウト:
// ┌───────────────┬───────────────┬──────────┐
// │  Claude 1     │  Claude 2     │ チート   │
// ├───────────────┴───────────────┤ シート   │
// │         Claude 3              │          │
// └───────────────────────────────┴──────────┘
//
// yazi/lazygitはフローティングで呼び出し:
//   Alt+y → yazi
//   Alt+g → lazygit

layout {
    pane split_direction="vertical" {
        // メインエリア（Claude Code 3ペイン）
        pane split_direction="horizontal" size="82%" {
            // 上段: Claude Code 1, 2
            pane split_direction="vertical" size="50%" {
                pane name="claude-1"
                pane name="claude-2"
            }
            // 下段: Claude Code 3（横幅フル）
            pane name="claude-3" size="50%"
        }
        // チートシート（右サイドバー）
        pane size="18%" name="cheatsheet" {
            command "bash"
            args "-c" "cat << 'EOF'
╔═══════════════════════════╗
║   チートシート            ║
╠═══════════════════════════╣
║ 🔧 フローティングツール   ║
╠═══════════════════════════╣
║ Alt+y   yazi (ファイラー) ║
║ Alt+g   lazygit (Git)     ║
║ Alt+t   ターミナル        ║
║ Esc     閉じる            ║
╠═══════════════════════════╣
║ 📐 ペイン操作             ║
╠═══════════════════════════╣
║ Ctrl+p → h/j/k/l 移動     ║
║ Ctrl+p → n       新規     ║
║ Ctrl+p → x       閉じる   ║
║ Ctrl+p → f       全画面   ║
║ Ctrl+p → w       浮動化   ║
║ Ctrl+p → d       切り離し ║
╠═══════════════════════════╣
║ 🔍 fzf検索                ║
╠═══════════════════════════╣
║ ff    ファイル検索        ║
║ rgs   内容検索            ║
║ rgi   対話検索            ║
║ fbr   ブランチ選択        ║
║ fwt   worktree選択        ║
║ fh    履歴検索            ║
╠═══════════════════════════╣
║ 📁 ファイル操作           ║
╠═══════════════════════════╣
║ y     yazi起動            ║
║ yy    yazi + cd           ║
║ lg    lazygit起動         ║
╚═══════════════════════════╝
EOF
exec tail -f /dev/null"
        }
    }
    // ステータスバー
    pane size=1 borderless=true {
        plugin location="compact-bar"
    }
}
